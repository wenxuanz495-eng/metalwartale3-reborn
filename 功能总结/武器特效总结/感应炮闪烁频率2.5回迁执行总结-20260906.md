# 感应炮（玄武家族）闪烁频率 2.5 回迁执行总结

执行日期：2026-09-06
任务：把感应炮四形态本体动画节奏恢复为 2.5 版本（灯光/烟雾/光束的每周期可见时长），**纯视觉，不改任何数值配置**。
前置文档：《玄武感应炮家族2.5与GitHub对比总结-20260906.md》
隔离工作区：`D:\superalloy\tmp-induction-family-restore\`

## 一、修改内容

文件：`swf\arms1100.swf`（源）、`build\swf\arms1100.swf`（运行）、`runtime\swf\arms1100.swf`（dev 暂存）

11.3 系把感应炮本体时间轴从 2.5 的 7/6/6/6 帧压缩成全 5 帧，缺失的是烟雾消散的中间保持帧和 lv1 的结尾帧。本次在四个本体 Sprite（1326/1320/1314/1308）内做帧级修复：

1. **插回烟雾保持帧**（四形态相同）：在第 2 帧（闪光+烟雾+音效）之后插入一帧，内容为 `PlaceObject2Tag depth=4 placeFlagMove ratio=21845`——烟雾变形推进到 1/3 且枪口闪光保持可见。
2. **修正现有推进帧的比例值**：原 ratio=32768（1/2）改为 **43691**（2/3），对齐 2.5 的三段式变形（0 → 1/3 → 2/3）。
3. **lv1 补回结尾空保持帧**（第 7 帧）。
4. `frameCount` 相应改为 7/6/6/6。

恢复后的 lv1 帧序（与 2.5 逐帧语义一致）：

```text
f1 本体+挂点
f2 移除本体、闪光(1324)+烟雾变形(1325)+开火音效(1240)
f3 烟雾 ratio=21845（闪光保持可见）        ← 本次插回
f4 移除闪光、本体复位、烟雾 ratio=43691
f5 移除烟雾、长光束(1306)出现
f6 光束换形(1307)
f7 空保持帧                                 ← 本次补回（仅 lv1）
```

实际效果：枪口闪光可见时长 1 帧 → **2 帧**（33ms→67ms）；烟雾消散 2 帧 → **3 帧**；长光束出现时机与 2.5 对齐（lv1 第 5 帧、lv2-4 第 5 帧→第 5 帧）；lv1 结尾与 2.5 相同多停 1 帧。

## 二、操作与验证记录

1. 备份当前 arms1100.swf（哈希 `282B9540...`，即含此前霰弹炮子弹回迁的状态）并记录；FFDec 导出 XML。
2. 取证确认两版差异本质：初始放置（ratio 0）两版一致；2.5 有 ratio=21845/43691 两步推进，GH 只有一帧 ratio=32768；lv1 另缺结尾帧。move 操作不带矩阵（此前对比中"scale(1.0,0.5)"为相邻标签误读，已澄清）。
3. 块内编辑：每 Sprite 断言 `ratio="32768"` 恰好 1 处后插入/替换/追加，`frameCount="5"` 改写；同一字符串对象写盘并回读自检。
4. `xml2swf` 重建（退出码 0）→ `swf2xml` 回读校验：
   - 四 Sprite frameCount = 7/6/6/6，ratio 序列 21845+43691 齐全、32768 消失；
   - lv1 逐帧走查与 2.5 帧序一致（见上）；
   - 霰弹炮子弹（41×22 位图）、霰弹炮/火神炮/小黄豆全部本体 Sprite 帧数改前后一致（15 个抽样逐字节同值）；
   - lv1 挂点 basePoint(204)/shootPoint(1294) 未动；
   - SymbolClass 前后 diff 完全一致（342 个导出）。
5. 部署三处哈希一致：`97B0094E7E793472160632BA7C6D8566B6C47090D7759542FA63F2C98808FC59`。
6. `config\build\current-resource-manifest.sha256` arms1100 行更新为新哈希。
7. 清理后台残留后 `构建.bat` 全量构建退出码 0；`build\swf`、`runtime\swf` 与源一致；`launch_game.bat --check sa` 通过。

## 三、涉及文件清单（均在 Git 工作区，未提交）

- `swf\arms1100.swf`、`build\swf\arms1100.swf`、`runtime\swf\arms1100.swf`（新哈希 `97B0094E...`）
- `config\build\current-resource-manifest.sha256`（arms1100 行）
- `build\game.swf` 等构建产物（已重新生成）
- 本文件为纯 SWF 资源修改，**未触碰** `2_EmbedXml_xmlClass3` 配置（爆能弹受击特效按用户要求暂不变）

## 四、遗留与实机验证清单

- [ ] 实机确认感应炮开火节奏：闪光更饱满（约 2 帧可见）、烟雾消散更完整、与 2.5 手感一致。
- [ ] 同文件此前已含霰弹炮子弹回迁（41×22 圆头弹），实机时顺带确认未被影响。
- [ ] 待决策的后续项（本任务未包含）：首发激光恢复（`index="1"`）+ 激光受击特效按 2.5 导入（sub1130 现存为 3.4 简化版，需从 sub37 导入 6 帧+音效闭包）。
- [ ] arms1100 的三批修改（火神炮名称+枪口点、霰弹炮子弹、本任务）均未 git 提交，建议实机确认后一起提交。
- 隔离工作区 `tmp-induction-family-restore` 保留，确认后可清理。
